<p id="notice"><%= notice %></p>

<p>
  <strong>Uid:</strong>
  <%= @group.uid %>
</p>

<% @messages.each do |message| %>
<div><%= message.author %>> <%= message.value %></div>
<% end %>
<%= link_to 'Edit', edit_group_path(@group) %> |
<%= link_to 'Back', groups_path %>
