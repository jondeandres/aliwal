#!/usr/bin/env ruby
$:.unshift(File.expand_path('../../lib', __FILE__))
require File.expand_path('../../config/environment', __FILE__)

require 'aliwal/zmq/receiver'

subscriber = Aliwal::ZMQ::Receiver.new
subscriber.bind
subscriber.subscribe
